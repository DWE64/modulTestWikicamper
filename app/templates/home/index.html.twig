{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block section %}
    <div class="row justify-content-center my-2">
        <div class="col-md-6">
            <h1 class="text-center my-2">Bienvenu, connectez-vous</h1>
            <div class="container my-2">
                <div class="row justify-content-center">
                    <div class="col-12 text-center my-2">
                    {% if is_granted('ROLE_OWNER') %}
                        <a class="btn btn-primary btn-lg" href="{{ path('app_rental_index') }}">Consulter mes véhicules</a>
                    {% elseif is_granted('ROLE_TENANT') %}
                        <a class="btn btn-primary btn-lg" href="{{ path('app_home_tenant') }}">Consulter les annonces</a>
                    {% else %}
                        <a class="btn btn-primary btn-lg" href="{{ path('app_login') }}">Se connecter</a>
                    {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
